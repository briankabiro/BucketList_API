language: python

python:
  - "3.6"

services:
  - postgresql

before_script:
  - psql -c 'create database test_db;' -U postgres 

env:
  - TEST_DATABASE_URI=postgresql://postgres:@localhost/test_db
  - SECRET_KEY=1213-bkajlkd-jkldajsflk

install: 
  - "pip install -r requirements.txt"
  - "pip install coveralls"
  - "pip install coverage"
  
script:
  - "nosetests --with-coverage --cover-erase -cover-package=app"

notifications:
  email: false

after_success:
  - coveralls